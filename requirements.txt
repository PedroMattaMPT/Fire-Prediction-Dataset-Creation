# Core dependencies for building the dataset.
# Equivalent to `pip install -e .` — use whichever you prefer.
pandas>=2.0
numpy>=1.24
geopandas>=1.0
shapely>=2.0
pyproj>=3.6
rasterio>=1.3
xarray>=2024.0
netCDF4>=1.6
dask>=2024.0
cdsapi>=0.7
openmeteo-requests>=1.2
requests-cache>=1.2
retry-requests>=2.0
openpyxl>=3.1
